<% if logged_in? %>
  <%= render "user_interface" %>
  <%= render "shared/feed" %>
<% else %>
  <div class="center jumbotron">
    <h1><%= t "static_pages.welcome" %></h1>
    <%= link_to t("static_pages.signup"), signup_path, class: "btn btn-lg btn-primary btn-success" %>
  </div>
<% end %>
